# Files preserved across `sysupgrade`. Same format as /etc/sysupgrade.conf
# — one path per line. OpenWrt's sysupgrade walks /lib/upgrade/keep.d/*
# and adds every line to the backup tar before writing the new image,
# then restores from the tar after first boot.
#
# What's in /etc/rasputin/:
#   - seed.env         operator-supplied controlplane URL + join token + bus pin
#                      (delivered by the controlplane's bootstrap_firewall
#                      job or hand-edited; MUST survive sysupgrade or
#                      every firmware update would reset the firewall
#                      back to unconfigured state).
#   - join.token       the bus join token, 0600, written by apply-seed from
#                      seed.env. The agent is given its PATH and re-reads it on
#                      every connect (geekdojo/geekdojo-brain#537). It must
#                      survive sysupgrade for the same reason seed.env does —
#                      /etc/config/rasputin is regenerated, and it no longer
#                      holds the token.
#   - agent-state/     rasputin-agent state (RASPUTIN_AGENT_STATE_DIR,
#                      set in files/etc/init.d/rasputin-agent). Holds the
#                      updater's pending/health-check bookkeeping, which
#                      must outlive the very sysupgrade reboot it tracks,
#                      and bus/pin: the bus pin a controlplane delivered
#                      over the bus to a node enrolled before pins existed
#                      (geekdojo/geekdojo-brain#448).
#   - deployment-mode  the deployment mode the controlplane last set through
#                      firewall.set_active, written by rasputin-agent. It
#                      decides the LAN DHCP role on every boot
#                      (files/etc/init.d/rasputin-dhcp-auto,
#                      geekdojo/geekdojo-brain#543): without it the box falls
#                      back to the boot-time probe, i.e. to whatever answers a
#                      DHCP DISCOVER first.
#
# The bus pin is trust material and survives sysupgrade through this entry
# twice over: a seeded pin as RASPUTIN_BUS_PIN in seed.env, a delivered one as
# agent-state/bus/pin. It is deliberately NOT in the system CA bundle, which a
# ca-bundle package upgrade rewrites and which holds CAs; a pin is not one.
#
# /etc/config/rasputin (UCI) is deliberately NOT in this list — it's
# regenerated from seed.env by /usr/lib/rasputin/apply-seed, which
# uci-defaults/99-rasputin runs on a fresh overlay. (An A/B slot update keeps
# the overlay, UCI included, and regenerates nothing.) Preserving it would just
# mask seed.env changes.
#
# Discovered during the CWWK x86-p5-n100 sysupgrade test 2026-06-07.
#
# ⚠️ LISTED FILE BY FILE, NOT AS `/etc/rasputin/`, AND trust/ IS DELIBERATELY
# ABSENT (geekdojo/geekdojo-brain#531).
#
# The trust anchor is the thing that decides which images this box will install.
# Preserving it made the anchor a property of the OVERLAY rather than of the
# image: whatever root-ca.pem a box happened to be carrying was copied forward
# over every sysupgrade, so a reflash — the one action an operator takes to get
# a box back to a known state — could not replace it. The image's own
# /etc/rasputin/trust/root-ca.pem, injected by CI from the org variable, is the
# anchor now, and a reflash is how you change it.
#
# That trades away in-place operator rotation of the root. It is deliberate: an
# anchor you cannot reset is worth less than one you must reflash to move, and
# rotation belongs to the release pipeline, not to a file that outlives the
# image it arrived with. /etc/init.d/rasputin-trust-clean removes any overlay
# copy already fielded (an A/B slot update keeps the whole overlay, so keep.d
# alone would never reach those boxes), logging loudly when it differed from the
# image's.
#
# Adding state under /etc/rasputin/? Add its path HERE. This list is no longer
# "the whole directory", so a new file is dropped by sysupgrade unless it is
# named — which is the cost of being able to leave trust/ out at all.
/etc/rasputin/seed.env
/etc/rasputin/join.token
/etc/rasputin/agent-state/
/etc/rasputin/hosts/

# Snort3 (IDS):
#   - /etc/config/snort        operator UCI tuning (changing home_net,
#                              flipping action, suppressing categories
#                              via include path, etc.) MUST survive
#                              sysupgrade. 99-rasputin only seeds initial
#                              values once on first boot via uci-defaults
#                              and then self-deletes, so the operator's
#                              tuning persists across reflashes only via
#                              this preservation.
#   - /etc/snort/rules/local.rules    operator's own custom rules,
#                              auto-picked-up by snort.uc's template
#                              loop. Bundled emerging-*.rules are NOT
#                              preserved — they refresh from the new
#                              image, which is the rule-update mechanism
#                              for v1 (independent rule pushes are a
#                              backlog item).
/etc/config/snort
/etc/snort/rules/local.rules

# Tailscale (mesh overlay):
#   - /etc/tailscale/             tailscaled state (machine key + headscale
#                                 node identity, state_file pinned in
#                                 /etc/config/tailscale). /var is tmpfs, so
#                                 this lives on the overlay; it MUST survive
#                                 sysupgrade or the firewall de-enrolls from
#                                 the mesh on every firmware update and would
#                                 need re-flashing the controlplane auth key.
#   - /etc/config/tailscale       our state-path + fw_mode pin. We ship it in
#                                 the overlay, but if the operator/controlplane
#                                 ever edits it (e.g. port), that survives too.
#   - /etc/rasputin/mesh/         per-installation Mesh CA bundle
#                                 (tailscaled-ca.pem, RASPUTIN_MESH_CA_BUNDLE),
#                                 written by rasputin-agent on enrollment and
#                                 trusted by tailscaled via SSL_CERT_FILE.
#                                 It is a per-installation CA the control plane
#                                 minted, not the release trust anchor, so it
#                                 survives where trust/ does not.
/etc/tailscale/
/etc/config/tailscale
/etc/rasputin/mesh/
