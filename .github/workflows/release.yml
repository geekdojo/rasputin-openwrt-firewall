name: release

# Build + sign + ship the Rasputin firewall (Node N) OpenWrt image.
#
# Mirror of rasputin-os's release workflow but adapted for OpenWrt:
#   - ImageBuilder, not Buildroot (~5-10 min build, not 50 min)
#   - sysupgrade .img.gz, not RAUC .raucb
#   - detached CMS .sig signature, verified by rasputin-agent on the
#     device before invoking sysupgrade (same YubiKey-rooted PKI as
#     rasputin-os; see os-images/release-pipeline.md §3)
#
# Triggers:
#   - push of a CalVer tag (e.g. 2026.06.0 or 2026.06.0-dev.1)
#                                       → validate + build + smoke + sign+release
#   - manual dispatch (full_build=true) → validate + build + smoke (no release)
#   - manual dispatch (full_build=false) → validate only (default, cheap)

on:
  push:
    tags:
      - "[0-9][0-9][0-9][0-9].[0-9][0-9].[0-9]*"
  workflow_dispatch:
    inputs:
      full_build:
        description: "Run the ImageBuilder build + smoke (defaults to validate-only)"
        type: boolean
        default: false

permissions:
  contents: write   # create releases + upload assets

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Shell script syntax
        run: |
          set -e
          for f in scripts/*.sh \
                   files/etc/init.d/* \
                   files/etc/uci-defaults/*; do
            test -f "$f" || continue
            echo "bash -n $f"; bash -n "$f"
          done
      - name: Repo structure
        run: |
          set -e
          req="README.md packages.txt scripts/init-imagebuilder.sh
               scripts/fetch-snort-rules.sh
               files/etc/init.d/rasputin-agent
               files/etc/uci-defaults/99-rasputin
               files/etc/snort/rasputin-extra.lua
               files/etc/rasputin/seed.env.template
               files/etc/rasputin/trust/README.md"
          for f in $req; do
            test -f "$f" && echo "ok  $f" || { echo "MISSING $f" >&2; exit 1; }
          done

  build:
    needs: validate
    # full_build arrives as a real boolean from the UI checkbox but as the
    # STRING "true" via the API / `gh -f`. Accept both. (Same gotcha as
    # rasputin-os.)
    if: ${{ startsWith(github.ref, 'refs/tags/') || (github.event_name == 'workflow_dispatch' && (inputs.full_build == true || inputs.full_build == 'true')) }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Compute version (CalVer)
        id: ver
        run: |
          if [ "${GITHUB_REF_TYPE}" = "tag" ]; then
            echo "version=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
            case "${GITHUB_REF_NAME}" in
              *-dev*) echo "channel=dev"    >> "$GITHUB_OUTPUT" ;;
              *)      echo "channel=stable" >> "$GITHUB_OUTPUT" ;;
            esac
          else
            echo "version=$(date -u +%Y.%m.0)-dev.${GITHUB_RUN_NUMBER}" >> "$GITHUB_OUTPUT"
            echo "channel=dev" >> "$GITHUB_OUTPUT"
          fi

      # Free space — ImageBuilder + downloaded packages comfortably fit on
      # a stock runner, but Suricata pulls in a sizable rule set and the
      # output image dir grows. Cheap insurance.
      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc \
            /opt/hostedtoolcache/CodeQL /usr/local/share/boost \
            "$AGENT_TOOLSDIRECTORY" 2>/dev/null || true
          df -h /

      # Cache the extracted ImageBuilder dir + the OpenWrt package
      # downloads it pulls during `make image`. Warm rebuilds skip the
      # ~200 MB IB tarball download AND the per-package downloads → a
      # warm CI run is ~3 min instead of ~10. Split restore/save with
      # if:always() so failed builds still warm the cache (same pattern
      # as rasputin-os).
      - name: Restore ImageBuilder + dl cache
        id: ibcache
        uses: actions/cache/restore@v4
        with:
          path: |
            imagebuilder
            imagebuilder/dl
          key: ib-${{ hashFiles('scripts/init-imagebuilder.sh', 'packages.txt') }}
          restore-keys: ib-

      - name: Init OpenWrt ImageBuilder (download if not cached)
        run: ./scripts/init-imagebuilder.sh

      # The agent binary lives in a PRIVATE control-plane release; same
      # auth dance as rasputin-os (fine-grained PAT, Contents:read on
      # geekdojo/rasputin-control-plane). The amd64 binary works on
      # OpenWrt's musl since rasputin-agent is CGO_ENABLED=0 static Go.
      - name: Fetch vendored rasputin-agent (private control-plane release)
        env:
          GH_TOKEN: ${{ secrets.CONTROL_PLANE_RELEASE_TOKEN }}
        run: |
          set -euo pipefail
          # Pinned agent version. Bump when the controlplane cuts a new
          # release; in the longer term this becomes a per-release input.
          # v0.2.0 (2026-06-08): adds the IDS subsystem (snort3 alert_fast
          # tailer + NATS publish on firewall-role agents). Required for
          # the IDS pipeline to fire end-to-end on the firewall image —
          # v0.1.0 didn't have it and was the silent-IDS bug on the CWWK
          # bring-up.
          # v0.2.1 (2026-06-09): adds the "ids: tailer started, path=...,
          # subject=..." startup log line so operators bisecting "is the
          # IDS subsystem alive?" can grep for it instead of resorting to
          # the manual `echo >> alert_fast.txt` bisect trick we needed on
          # the dev.1 → dev.2 bring-up.
          # v0.5.0 (2026-06-12): the REAL OpenWrt ubus/UCI backend — the
          # agent now applies firewall intents to live UCI + kernel nftables
          # (autodetects uci on PATH + /etc/config/firewall present) instead
          # of the file-backed mock. Hardware-validated on the CWWK. Also
          # carries sd_notify support (no-op under procd) and the IDS path
          # unchanged. This is the build that makes the firewall tab real on
          # shipped images.
          # v0.6.0 (2026-06-13): bus join-token auth — the agent presents its
          # node id + RASPUTIN_CP_JOIN_TOKEN to the controlplane's NATS
          # auth-callout, which mints a per-node subject-scoped credential.
          # No-op until the controlplane runs RASPUTIN_BUS_AUTH=enforce; also
          # bakes /etc/rasputin/image-version (first firewall image to
          # populate the node view's OS-image field).
          # v0.7.0 (2026-06-13): mDNS resolution of rasputin.local — the OpenWrt
          # firewall has no .local resolver, so this is what lets the agent reach
          # nats://rasputin.local:4222 in a provisioned matched set; plus
          # node-bound join tokens. REQUIRED for the matched-set firewall.
          # v0.7.1 (2026-06-13): mDNS multi-homed fix — query on EVERY interface,
          # not just the default multicast route (which the firewall sends out its
          # WAN, where the control plane isn't). Without this, rasputin.local never
          # resolved on the firewall. Confirmed on the bench.
          # v0.8.0 (2026-06-17): mesh goes real — agent installs the per-installation
          # Mesh CA (appends to /etc/ssl/certs/ca-certificates.crt on OpenWrt) and
          # runs real `tailscale up` against the self-hosted Headscale. Pairs with
          # the tailscale/kmod-tun/ca-bundle packages added to packages.txt.
          VER=0.8.0
          mkdir -p /tmp/agent files/usr/bin
          gh release download "v$VER" \
            --repo geekdojo/rasputin-control-plane \
            --pattern "rasputin-agent-${VER}-linux-amd64.tar.gz" \
            --dir /tmp/agent --clobber
          tar -xzf /tmp/agent/rasputin-agent-${VER}-linux-amd64.tar.gz \
            -C files/usr/bin
          chmod +x files/usr/bin/rasputin-agent
          file files/usr/bin/rasputin-agent | head -1

      - name: Inject trust root from org variable
        run: |
          mkdir -p files/etc/rasputin/trust
          printf '%s' "${{ vars.RASPUTIN_ROOT_CA_PEM }}" \
            > files/etc/rasputin/trust/root-ca.pem
          if [ ! -s files/etc/rasputin/trust/root-ca.pem ]; then
            echo "::warning::RASPUTIN_ROOT_CA_PEM org variable is empty — image will ship without a verifiable trust root"
          fi

      # Bake root's SSH authorized_keys from an org/repo variable so the firewall
      # ships key-only (the 96-rasputin-dropbear-harden uci-defaults disables
      # password auth when this file is non-empty) — no LAN-reachable passwordless
      # root. Empty variable → empty file → harden script leaves password auth on
      # and warns (dev image, avoids a lockout). Mirrors the trust-root pattern.
      - name: Inject SSH authorized key (key-only SSH)
        env:
          SSH_KEY: ${{ vars.RASPUTIN_SSH_AUTHORIZED_KEY }}
        run: |
          mkdir -p files/etc/dropbear
          if printf '%s' "$SSH_KEY" | grep -q '[^[:space:]]'; then
            printf '%s\n' "$SSH_KEY" > files/etc/dropbear/authorized_keys
            chmod 600 files/etc/dropbear/authorized_keys
            echo "baked $(grep -c . files/etc/dropbear/authorized_keys) authorized SSH key line(s) — image is key-only"
          else
            : > files/etc/dropbear/authorized_keys
            echo "::warning::RASPUTIN_SSH_AUTHORIZED_KEY is empty — image ships with passwordless root (DEV ONLY). Set the variable to enable key-only SSH."
          fi

      # Bake the pinned ET Open snort3 ruleset into files/etc/snort/.
      # ImageBuilder consumes files/ verbatim — the rules must be on disk
      # BEFORE `make image` runs. The script is idempotent and pins the
      # tarball SHA; bump PINNED_SHA in the script when refreshing rules.
      - name: Fetch Snort3 Community Rules
        run: ./scripts/fetch-snort-rules.sh

      # Bake the OS image version (CalVer) into the overlay. The agent reads
      # /etc/rasputin/image-version at startup and reports it on registration
      # so the control-plane UI can show which image each node runs — critical
      # for troubleshooting and support. The Buildroot OS writes the same file
      # at its build time. Build-generated, NOT committed (a static file would
      # go stale); files/etc/rasputin/image-version is gitignored.
      - name: Bake image version into overlay
        run: |
          mkdir -p files/etc/rasputin
          printf '%s\n' "${{ steps.ver.outputs.version }}" \
            > files/etc/rasputin/image-version

      - name: ImageBuilder build
        env:
          PROFILE: generic
        run: |
          set -euo pipefail
          # Build the PACKAGES list from packages.txt:
          #   1. sed strips trailing inline `# ...` comments per line. Without
          #      this, bash treats embedded `(...)` in a comment as a subshell
          #      and `make image PACKAGES='...'` blows up.
          #   2. grep removes the now-empty / whitespace-only lines (full-line
          #      comments become empty after step 1).
          #   3. tr collapses to a single space-separated list.
          pkgs=$(sed 's/[[:space:]]*#.*$//' packages.txt \
                 | grep -vE '^[[:space:]]*$' \
                 | tr '\n' ' ')
          echo "PACKAGES: $pkgs"
          files_abs=$(realpath files)
          # ImageBuilder's `make image` is the one-shot entrypoint.
          cd imagebuilder
          make image \
            PROFILE="$PROFILE" \
            PACKAGES="$pkgs" \
            FILES="$files_abs" \
            EXTRA_IMAGE_NAME=rasputin \
            -j"$(nproc)"
          echo
          echo "----- output artifacts -----"
          ls -lh bin/targets/x86/64/

      - name: Save ImageBuilder + dl cache
        if: always() && steps.ibcache.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: |
            imagebuilder
            imagebuilder/dl
          key: ib-${{ hashFiles('scripts/init-imagebuilder.sh', 'packages.txt') }}

      - name: Patch GRUB cmdline (i915.modeset=0)
        run: |
          set -euo pipefail
          # mtools (mcopy/mdir) is the tool we use to read/write FAT EFI
          # partition contents without losetup/mount. fdisk for finding
          # the partition offset.
          sudo apt-get install -y --no-install-recommends mtools fdisk
          src=$(ls imagebuilder/bin/targets/x86/64/*squashfs-combined-efi.img.gz | head -1)
          test -n "$src" || { echo "::error::squashfs-combined-efi not produced"; exit 1; }
          # Decompress, patch grub.cfg in place, re-compress (overwriting
          # the original artifact so the stage step picks it up).
          # OpenWrt's combined-efi .img.gz has trailing NUL padding, so
          # gunzip returns rc=2 ("trailing garbage ignored") even though
          # decompression succeeded. Same gotcha the smoke step works
          # around — tolerate rc=2 here too.
          rc=0
          gunzip -k -f "$src" || rc=$?
          if [ "$rc" -ne 0 ] && [ "$rc" -ne 2 ]; then
            echo "::error::gunzip failed with rc=$rc on $src"
            exit "$rc"
          fi
          img="${src%.gz}"
          ./scripts/patch-image-cmdline.sh "$img"
          gzip -f "$img"

      - name: Stage release-shaped names + upload artifact
        run: |
          set -euo pipefail
          mkdir -p staged
          ver="${{ steps.ver.outputs.version }}"
          # On x86 OpenWrt there is NO separate `sysupgrade` artifact —
          # the same combined image is used for initial flash (write to
          # disk) and for `sysupgrade -F` consumption later. ImageBuilder
          # produces two rootfs flavors:
          #   - *-squashfs-combined-efi.img.gz → squashfs base + /overlay
          #     (atomic-ish upgrades, config preserved across sysupgrade).
          #     This is the canonical firewall format.
          #   - *-ext4-combined-efi.img.gz     → writable ext4 root (no
          #     /overlay split). Easier to mess with at the cost of
          #     non-atomic upgrade semantics. Not what we want.
          # Pick squashfs+efi explicitly so the choice doesn't depend on
          # glob ordering.
          src=$(ls imagebuilder/bin/targets/x86/64/*squashfs-combined-efi.img.gz | head -1)
          test -n "$src" || { echo "::error::squashfs-combined-efi not produced"; exit 1; }
          cp "$src" "staged/rasputin-fw-n100-${ver}-combined-efi.img.gz"
          # ImageBuilder writes a .manifest file in bin/targets/x86/64/
          # listing every package + version installed in the image. This
          # IS our SBOM — authoritative, deterministic, OpenWrt-native.
          # The manifest is shared across rootfs flavors (squashfs/ext4
          # don't differ in package set), so the filename is just
          # `openwrt-<ver>-<arch>-<profile>.manifest` — no flavor suffix.
          # We stage it under the release-shaped name so the canary
          # workflow can fetch it from the latest stable to diff against.
          manifest_src=$(ls imagebuilder/bin/targets/x86/64/*.manifest 2>/dev/null | head -1)
          test -n "$manifest_src" || { echo "::error::ImageBuilder manifest not produced"; exit 1; }
          cp "$manifest_src" "staged/rasputin-fw-n100-${ver}.sbom.txt"
          ls -lh staged/

      - uses: actions/upload-artifact@v4
        with:
          name: fw-n100-${{ steps.ver.outputs.version }}
          path: |
            staged/*.img.gz
            staged/*.sbom.txt

  smoke-fw:
    needs: build
    # Run as long as the workflow wasn't cancelled. Boots the combined-efi
    # image under QEMU+OVMF and waits for OpenWrt's procd banner / login
    # prompt — that's enough to confirm the image isn't structurally broken.
    # It does NOT validate the i226-V driver, line-rate, or NAT — that's
    # hardware-only.
    if: ${{ !cancelled() && (startsWith(github.ref, 'refs/tags/') || (github.event_name == 'workflow_dispatch' && (inputs.full_build == true || inputs.full_build == 'true'))) }}
    runs-on: ubuntu-latest
    steps:
      - name: Install QEMU + OVMF
        run: sudo apt-get update && sudo apt-get install -y --no-install-recommends qemu-system-x86 ovmf
      - uses: actions/download-artifact@v4
        with:
          pattern: fw-n100-*
          merge-multiple: true
          path: img
      - name: Boot headless, wait for OpenWrt banner
        run: |
          set -x
          # Pick the combined-efi (flashable) variant; sysupgrade isn't
          # bootable standalone.
          IMG_GZ=$(ls img/*combined-efi*.img.gz | head -1)
          test -n "$IMG_GZ" || { echo "no combined-efi .img.gz artifact"; exit 1; }
          IMG=${IMG_GZ%.gz}
          # OpenWrt's combined-efi .img.gz has trailing NUL padding after the
          # gzip stream (raw-image alignment). gunzip decompresses correctly
          # but exits 2 ("trailing garbage ignored"). Tolerate exit 2; only
          # fail on real errors or a missing output file.
          gunzip -kf "$IMG_GZ" || rc=$?
          if [ "${rc:-0}" -ne 0 ] && [ "${rc:-0}" -ne 2 ]; then
            echo "::error::gunzip failed with rc=${rc}"; exit "${rc}"
          fi
          [ -s "$IMG" ] || { echo "::error::decompressed image $IMG missing or empty"; exit 1; }
          # Auto-pick non-secboot OVMF (Ubuntu renamed the files to _4M).
          OVMF_CODE=$(ls /usr/share/OVMF/OVMF_CODE*.fd 2>/dev/null | grep -v secboot | head -1)
          OVMF_VARS=$(ls /usr/share/OVMF/OVMF_VARS*.fd 2>/dev/null | grep -v secboot | head -1)
          cp "$OVMF_VARS" /tmp/vars.fd
          ( timeout 240 qemu-system-x86_64 \
              -machine q35 -m 512 -smp 2 -cpu max -nographic \
              -drive if=pflash,format=raw,readonly=on,file="$OVMF_CODE" \
              -drive if=pflash,format=raw,file=/tmp/vars.fd \
              -drive file="$IMG",format=raw,if=virtio \
              -serial mon:stdio > /tmp/console.log 2>&1 ) &
          QPID=$!
          ok=0
          for _ in $(seq 1 100); do
            if grep -qE "Please press Enter to activate this console|BusyBox v[0-9]|procd: - init complete -" /tmp/console.log; then
              ok=1; break
            fi
            sleep 2
          done
          kill "$QPID" 2>/dev/null || true
          echo "----- last 80 lines of console -----"; tail -80 /tmp/console.log || true
          [ "$ok" = 1 ] || { echo "::error::firewall image did not reach OpenWrt banner in QEMU"; exit 1; }
          echo "OpenWrt firewall image booted in QEMU"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: smoke-fw-console
          path: /tmp/console.log

  sign-and-release:
    needs: [build, smoke-fw]
    if: ${{ startsWith(github.ref, 'refs/tags/') }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Compute version
        id: ver
        run: |
          VERSION="${GITHUB_REF_NAME}"
          case "$VERSION" in
            *-dev*) CHANNEL=dev    ; PRERELEASE=true  ;;
            *)      CHANNEL=stable ; PRERELEASE=false ;;
          esac
          echo "version=$VERSION"       >> "$GITHUB_OUTPUT"
          echo "channel=$CHANNEL"       >> "$GITHUB_OUTPUT"
          echo "prerelease=$PRERELEASE" >> "$GITHUB_OUTPUT"

      - uses: actions/download-artifact@v4
        with:
          pattern: fw-n100-*
          merge-multiple: true
          path: artifacts

      - name: Materialise signing material to tmpfs
        env:
          LEAF_KEY:  ${{ secrets.RASPUTIN_LEAF_KEY }}
          LEAF_CERT: ${{ secrets.RASPUTIN_LEAF_CERT }}
          ROOT_PEM:  ${{ vars.RASPUTIN_ROOT_CA_PEM }}
        run: |
          set -eu
          if [ -z "${LEAF_KEY:-}" ] || [ -z "${LEAF_CERT:-}" ] || [ -z "${ROOT_PEM:-}" ]; then
            echo "::error::missing one of RASPUTIN_LEAF_KEY / RASPUTIN_LEAF_CERT (repo secrets) or RASPUTIN_ROOT_CA_PEM (org variable)"; exit 1
          fi
          sudo mkdir -p /mnt/rasputin-sign
          sudo mount -t tmpfs -o size=8M,mode=0700,uid=$(id -u),gid=$(id -g) tmpfs /mnt/rasputin-sign
          umask 077
          printf '%s' "$LEAF_KEY"  > /mnt/rasputin-sign/leaf.key
          printf '%s' "$LEAF_CERT" > /mnt/rasputin-sign/leaf-chain.pem
          printf '%s' "$ROOT_PEM"  > /mnt/rasputin-sign/root-ca.pem
          # Split leaf-chain into leaf + intermediate (same pattern as
          # rasputin-os; CMS signing needs them separately).
          awk -v leaf=/mnt/rasputin-sign/leaf.pem \
              -v inter=/mnt/rasputin-sign/intermediate.pem \
              '/-----BEGIN CERTIFICATE-----/{i++} {if(i==1)print > leaf; else if(i==2)print > inter}' \
              /mnt/rasputin-sign/leaf-chain.pem
          openssl verify -CAfile /mnt/rasputin-sign/root-ca.pem \
            -untrusted /mnt/rasputin-sign/intermediate.pem \
            /mnt/rasputin-sign/leaf.pem \
            || { echo "::error::leaf cert chain does not verify against root"; exit 1; }

      - name: CMS-sign images + emit manifest.json
        run: |
          set -eu
          mkdir -p release-out
          # The SBOM (.sbom.txt = OpenWrt package manifest) ships
          # alongside the signed images. The canary workflow fetches
          # this from the most recent stable release to diff against
          # the current build's manifest.
          cp artifacts/*.sbom.txt release-out/ 2>/dev/null || true
          : > /tmp/manifest-artifacts.jsonl
          ver="${{ steps.ver.outputs.version }}"
          for img in artifacts/*.img.gz; do
            [ -f "$img" ] || continue
            name=$(basename "$img")
            cp "$img" "release-out/$name"
            sig="release-out/${name}.sig"
            # Detached CMS signature, DER format. The rasputin-agent on
            # the device verifies with:
            #   openssl cms -verify -binary -inform DER \
            #     -in <sig> -content <img.gz> \
            #     -CAfile /etc/rasputin/trust/root-ca.pem
            openssl cms -sign -binary \
              -in "release-out/$name" \
              -signer  /mnt/rasputin-sign/leaf.pem \
              -certfile /mnt/rasputin-sign/intermediate.pem \
              -inkey   /mnt/rasputin-sign/leaf.key \
              -outform DER \
              -out "$sig"

            sha=$(sha256sum "release-out/$name" | awk '{print $1}')
            sigsha=$(sha256sum "$sig"            | awk '{print $1}')
            sz=$(stat -c%s "release-out/$name")
            sigsz=$(stat -c%s "$sig")
            kind=$(echo "$name" | grep -oE 'combined-efi|sysupgrade')
            built=$(date -u +%Y-%m-%dT%H:%M:%SZ)

            jq -nc \
              --arg sku fw-n100 --arg arch amd64 \
              --arg compatible rasputin-fw-n100 \
              --arg kind "$kind" \
              --arg image "$name"            --arg sha "$sha"    --argjson sz "$sz" \
              --arg sig   "$(basename "$sig")" --arg sigsha "$sigsha" --argjson sigsz "$sigsz" \
              --arg signedBy "Rasputin Release ${ver}" --arg built "$built" \
              '{sku:$sku, architecture:$arch, compatible:$compatible, kind:$kind,
                image:$image, sha256:$sha, sizeBytes:$sz,
                sig:$sig, sigSha256:$sigsha, sigSizeBytes:$sigsz,
                signedBy:$signedBy, buildDate:$built}' \
              >> /tmp/manifest-artifacts.jsonl
          done

          jq -n \
            --arg version "${{ steps.ver.outputs.version }}" \
            --arg channel "${{ steps.ver.outputs.channel }}" \
            --slurpfile artifacts /tmp/manifest-artifacts.jsonl \
            '{version:$version, channel:$channel, artifacts:$artifacts}' \
            > release-out/manifest.json

          echo "----- release-out/ -----" && ls -lh release-out/
          echo "----- manifest.json -----" && cat release-out/manifest.json

      - name: Wipe signing material
        if: always()
        run: sudo umount /mnt/rasputin-sign 2>/dev/null || true

      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -eu
          PRERELEASE_FLAG=""
          if [ "${{ steps.ver.outputs.prerelease }}" = "true" ]; then
            PRERELEASE_FLAG="--prerelease"
          fi
          gh release create "${{ steps.ver.outputs.version }}" \
            --title "Rasputin Firewall ${{ steps.ver.outputs.version }}" \
            --notes "Channel: \`${{ steps.ver.outputs.channel }}\`. Verify each image's signature with the public root CA (\`vars.RASPUTIN_ROOT_CA_PEM\` on the geekdojo org) — \`openssl cms -verify -binary -inform DER -in <image>.img.gz.sig -content <image>.img.gz -CAfile root-ca.pem\`. Per-asset SHA-256 in \`manifest.json\`. The \`*.sbom.txt\` asset is the package manifest (canary workflow uses this to detect upstream drift)." \
            $PRERELEASE_FLAG \
            release-out/*

      - name: Attempt to lock release as immutable
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -eu
          # Process discipline: once a tag is published, it MUST NOT be
          # re-cut. GitHub's immutable-releases feature can enforce this
          # at the API level (no edits, no deletion). We PATCH the
          # release with immutable=true right after publishing.
          #
          # IMPORTANT: as of 2026.06.1 bring-up, this PATCH succeeds at
          # the HTTP level but does NOT actually flip the field on
          # private org repos without the immutable-releases feature
          # toggled on at the repo or org level. The API silently
          # accepts the request and leaves immutable=false. So this
          # step is currently an opt-in best-effort:
          #   - If the repo/org has immutable releases enabled
          #     (Settings → General → Releases, or org-tier toggle),
          #     this PATCH will succeed and lock the release.
          #   - If not, we print a ::warning:: that we couldn't lock —
          #     no failure, but visibility in CI logs.
          #
          # Process discipline (never delete/re-cut a tag) is the
          # primary protection regardless. Tag protection rules
          # (Settings → Tags) are a more reliable backstop on
          # this repo/org tier.
          db_id=$(gh api "repos/$GITHUB_REPOSITORY/releases/tags/${{ steps.ver.outputs.version }}" --jq .id)
          result=$(gh api "repos/$GITHUB_REPOSITORY/releases/$db_id" \
                     -X PATCH \
                     -f immutable=true \
                     --jq '.immutable')
          if [ "$result" = "true" ]; then
            echo "::notice::release ${{ steps.ver.outputs.version }} locked immutable=true"
          else
            echo "::warning::PATCH immutable=true did not take effect on this release (immutable=$result). The feature likely needs enablement at Settings → General → Releases or via org-tier toggle. Process discipline + Settings → Tags protection rules are the working backstop until then."
          fi
